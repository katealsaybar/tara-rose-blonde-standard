# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*~
.vscode/

# Never commit
.env
.env.*

# Meeting recordings and transcripts. This repo is public and its Pages site
# will be live.
*Personal Meeting Room*
*.vtt
*.transcript.txt

# Internal build brief: quotes a private meeting, and records which stylists
# are and are not signed off on colour. Lives beside the code, never ships.
docs/BUILD-PROMPT.md

# The blonde extraction is internal. It never ships, wherever a copy lands.
TARA_BLONDE_RAW.md

# Internal pricing. The page ships with zero price tokens and this repo is
# public: no Phorest export, no price grid, ever.
docs/BLONDE-PRICES-FROM-PHOREST.md
*PRICES*
*prices*

# Stylist capability documents. Never in a public repo, wherever they land.
*stylist*capabilit*
*sign-off*
*signoff*
